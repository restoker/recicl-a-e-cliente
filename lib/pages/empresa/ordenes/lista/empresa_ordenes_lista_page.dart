import 'package:flutter/material.dart';

class EmpresaOrdenesListaPage extends StatelessWidget {
  const EmpresaOrdenesListaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Desde EmpresaOrdenesListaPage'),
      ),
    );
  }
}
