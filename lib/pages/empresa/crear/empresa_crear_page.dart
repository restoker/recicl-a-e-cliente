import 'package:flutter/material.dart';

class EmpresaCrearPage extends StatelessWidget {
  const EmpresaCrearPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Desde EmpresaCrearPage'),
      ),
    );
  }
}
