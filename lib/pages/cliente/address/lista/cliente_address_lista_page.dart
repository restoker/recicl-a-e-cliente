import 'package:flutter/material.dart';

class ClienteAddressListaPage extends StatelessWidget {
  const ClienteAddressListaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Desde ClienteAddressListaPage'),
      ),
    );
  }
}
