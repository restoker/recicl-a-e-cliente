import 'package:flutter/material.dart';

class ClienteUpdatePage extends StatelessWidget {
  const ClienteUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Desde ClienteUpdatePage'),
      ),
    );
  }
}
